name: Deploy Production (Industry Standard)

on:
  workflow_dispatch:
    inputs:
      deployment_type:
        description: 'Choose deployment type'
        required: true
        default: 'simple'
        type: choice
        options:
        - simple
        - full
  push:
    branches: [ main ]

env:
  PROJECT_ID: direct-glider-465821-p7
  REGION: us-east5
  SERVICE_NAME: realm-rivalry-backend
  IMAGE_NAME: us-east5-docker.pkg.dev/direct-glider-465821-p7/realm-rivalry/backend

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Verify latest code and build context
      run: |
        echo "🔍 VERIFYING DEPLOYMENT USES LATEST CODE AND BUILD CONTEXT"
        echo "Current commit: $(git rev-parse HEAD)"
        echo "Latest commit message: $(git log -1 --pretty=format:'%s')"
        echo "Git status check:"
        git status --porcelain || echo "No uncommitted changes"
        echo ""
        echo "🔍 DOCKERFILE VERIFICATION:"
        grep -A 3 "Copy source code" Dockerfile.backend || echo "⚠️ Dockerfile pattern not found"
        echo ""
        echo "🔍 BUILD CONTEXT SOURCE CODE VERIFICATION:"
        echo "Health endpoint actual content:"
        grep -A 5 -B 5 "environment:" server/health.ts || echo "⚠️ Environment object not found in build context"
        echo ""
        echo "CORS configuration actual content:"  
        grep -A 10 -B 2 "COMPREHENSIVE CORS" server/index.ts || echo "⚠️ CORS config not found in build context"
        echo ""
        echo "🔍 FILE INTEGRITY CHECK:"
        echo "server/health.ts last modified: $(stat -c %y server/health.ts)"
        echo "server/index.ts last modified: $(stat -c %y server/index.ts)"
        echo ""
        echo "✅ Build context verification complete"

    - name: Ensure latest Git state
      run: |
        echo "🔄 ENSURING LATEST GIT STATE FOR BUILD CONTEXT"
        git fetch origin main
        git reset --hard origin/main
        echo "Git head after reset: $(git rev-parse HEAD)"
        echo "Working directory clean: $(git status --porcelain | wc -l) uncommitted files"
        
    - name: Final build context verification
      run: |
        echo "🔍 FINAL BUILD CONTEXT VERIFICATION BEFORE DOCKER BUILD"
        echo "Environment object in server/health.ts:"
        grep -n "environment:" server/health.ts || echo "❌ CRITICAL: Environment object missing"
        echo ""
        echo "CORS config in server/index.ts:"
        grep -n "COMPREHENSIVE CORS" server/index.ts || echo "❌ CRITICAL: CORS config missing"
        echo ""
        if grep -q "environment:" server/health.ts && grep -q "COMPREHENSIVE CORS" server/index.ts; then
          echo "✅ Build context contains latest source code"
        else
          echo "❌ CRITICAL ERROR: Build context missing latest changes"
          echo "This deployment will fail - stopping here"
          exit 1
        fi

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Configure Docker authentication
      run: gcloud auth configure-docker us-east5-docker.pkg.dev

    - name: Build production image
      run: |
        echo "Building production image with FIXED Dockerfile.backend..."
        echo "🔥 FORCE NO-CACHE BUILD to ensure latest code is used"
        docker build \
          --no-cache \
          --platform linux/amd64 \
          -f Dockerfile.backend \
          -t ${{ env.IMAGE_NAME }}:latest .

    - name: Push image to registry
      run: docker push ${{ env.IMAGE_NAME }}:latest

    - name: Deploy to Cloud Run
      run: |
        echo "Deploying to Cloud Run with DIRECT traffic routing..."
        echo "🔄 FORCE COMPLETE REFRESH - ensuring latest code deploys with immediate traffic"
        gcloud run deploy ${{ env.SERVICE_NAME }} \
          --image ${{ env.IMAGE_NAME }}:latest \
          --platform managed \
          --region ${{ env.REGION }} \
          --allow-unauthenticated \
          --set-env-vars NODE_ENV=production \
          --set-secrets DATABASE_URL_PRODUCTION=database-url:latest,VITE_FIREBASE_API_KEY=firebase-api-key:latest,VITE_FIREBASE_PROJECT_ID=firebase-project-id:latest,VITE_FIREBASE_APP_ID=firebase-app-id:latest \
          --memory 2Gi \
          --cpu 2 \
          --concurrency 80 \
          --max-instances 10 \
          --min-instances 1 \
          --port 8080 \
          --timeout 300s \
          --cpu-boost

    - name: Verify deployment
      run: |
        echo "Getting service URL..."
        SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.REGION }} --format="value(status.url)")
        echo "Service URL: $SERVICE_URL"
        
        echo "Testing health endpoint..."
        curl -f "$SERVICE_URL/health" || (echo "Health check failed" && exit 1)
        
        echo "🔍 VERIFYING ENHANCED DEBUGGING DEPLOYED:"
        echo "Testing deployment verification endpoint..."
        curl -s "$SERVICE_URL/api/deployment-verification" | jq '.deploymentStatus' || echo "⚠️ Deployment verification endpoint missing"
        
        echo "Testing enhanced health endpoint version..."
        curl -s "$SERVICE_URL/health" | jq '.version' || echo "⚠️ Version missing"
        
        echo "Testing CORS headers..."
        curl -H "Origin: https://www.realmrivalry.com" -D - "$SERVICE_URL/health" | grep -i access-control || echo "⚠️ CORS headers missing"
        
        echo "✅ Backend deployment successful!"
        echo "Backend available at: $SERVICE_URL"
        echo "Note: Frontend deployed separately via 'Frontend Only - Firebase Deploy' workflow"